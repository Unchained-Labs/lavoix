.venv/
__pycache__/
.pytest_cache/
*.pyc
.env
dist/
build/
*.egg-info/
.env

# Deployment-specific, not upstream's business. chezmoi's box moves the
# published port off 8090 (its dashboard owns that) and attaches this to a
# shared docker network. Another deployment will want different answers.
docker-compose.override.yml
